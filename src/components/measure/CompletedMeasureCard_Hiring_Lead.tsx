
import React from 'react';
import { CompletedMeasureCard } from './CompletedMeasureCard';

export const CompletedMeasureCard_Hiring_Lead = ({ className }: { className?: string }) => {
  return <CompletedMeasureCard className={className} />;
};
