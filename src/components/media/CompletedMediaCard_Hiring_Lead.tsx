
import React from 'react';
import { CompletedMediaCard } from './CompletedMediaCard';

export const CompletedMediaCard_Hiring_Lead = ({ className }: { className?: string }) => {
  return <CompletedMediaCard className={className} />;
};
